# Architecture docs

The source of truth for this project is `SatQuery_Architecture.pdf` (Architecture B),
referenced throughout `CLAUDE.md`. It has not yet been added to this repo.

Place it here as `SatQuery_Architecture.pdf` before starting any stage whose scope
depends on architectural details beyond the frozen facts already captured in
`CLAUDE.md` §1.
