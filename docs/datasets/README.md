# Dataset cards

Per CLAUDE.md §7, every dataset used in this project gets a card in this directory
recording: source, licence, sample count, splits, class distribution, known
limitations, version/date, and how it is used in the pipeline.

One markdown file per dataset, e.g. `docs/datasets/<dataset-name>.md`.

No dataset cards exist yet.
